<nav class="breadcrumbs" style="margin-bottom:20px;font-size:14px;color:#777;">
  <a href="/" style="color:#2e7d32;text-decoration:none;">Home</a>
  {% if collection %}
    <span> / </span>
    <a href="{{ collection.url }}" style="color:#2e7d32;text-decoration:none;">{{ collection.title }}</a>
  {% endif %}
  {% if product %}
    <span> / </span>
    <span>{{ product.title }}</span>
  {% endif %}
</nav>
